services:
  postgres:
    image: postgres:16
    container_name: finsprint-db
    environment:
      POSTGRES_DB: finsprintdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ilikepizza
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
